#I. Exercise programs on basic control structures & loops.
#a) Write a program for checking the given number is even or odd.
#number = int(input("Enter a number: "))
#if number % 2 == 0:
   # print(f"{number} is even.")
#else:
    #print(f"{number} is odd.")
#b) Using a for loop, write a program that prints the decimal equivalents of 1/2, 1/3, 1/4 ,....... 1/10

#for denominator in range(2, 11):

    #decimal_value = 1 / denominator
    
    
   # print("The decimal equivalent of 1/{0} is {1:.4f}".format(denominator, decimal_value))
   
#c) Write a program for displaying reversal of a number.

#def reverse_number(num):

   # if num[0] == '-':
        
      #  reversed_num = '-' + num[:0:-1]
  #  else:
        
       # reversed_num = num[::-1]
   # return reversed_num


#if __name__ == "__main__":
    # Take input from the user
   # number = input("Enter a number: ")
    
    
   # reversed_number = reverse_number(number)
    

   # print("The reversal of the number is:", reversed_number)
   
# d) Write a program for finding biggest number among 3 numbers.

def find_largest(num1, num2, num3):

    largest = max(num1, num2, num3)
    return largest


if __name__ == "__main__":
    # Take input from the user
    number1 = float(input("Enter the first number: "))
    number2 = float(input("Enter the second number: "))
    number3 = float(input("Enter the third number: "))
    
    
    largest_number = find_largest(number1, number2, number3)
    
    
    print("The largest number among the three is:", largest_number)


